# --- Conlang Studio Example Environment Variables ---
# Copy this file to `.env.local` (for Next.js) or `.env` for scripts.
# Never commit real secrets. All values here are safe defaults for local dev.

# Node environment
NODE_ENV=development

# Application base URL (used by frontend when constructing absolute links)
NEXT_PUBLIC_APP_BASE_URL=http://localhost:3000

# API base (if you later split API service)
# NEXT_PUBLIC_API_BASE_URL=http://localhost:3000

# Build / runtime metadata (populated by build pipelines if desired)
APP_BUILD_SHA=dev-local
APP_BUILD_TIME=local

# Logging
LOG_LEVEL=info

# Database (PostgreSQL)
DB_HOST=localhost
DB_PORT=5432
DB_USER=app
DB_PASSWORD=dev
DB_NAME=conlang_studio

# Optional: override for migrations if separate shadow DB
# DB_MIGRATIONS_DATABASE=conlang_studio

# Metrics / Observability (future placeholders)
METRICS_ENABLED=true
METRICS_EXPORT_INTERVAL_MS=10000

# Feature flags (Phase 1+ scaffolding)
FEATURE_SEMANTICS_UI=true
FEATURE_MORPHOLOGY_UI=true
FEATURE_VALIDATORS_PANEL=true

# Replay / determinism (Phase 1+ placeholder)
REPLAY_SEED=42

# Security placeholders (DO NOT use these values in prod)
JWT_SECRET=dev-insecure-secret-change-me
SESSION_TTL_SECONDS=86400

# CORS (adjust if external domains access API)
ALLOWED_ORIGINS=http://localhost:3000

# Rate limiting (future)
RATE_LIMIT_WINDOW_SECONDS=60
RATE_LIMIT_MAX_REQUESTS=300

# CLI / Scripts behavior
NON_INTERACTIVE=true

# --- Notes ---
# 1. For Next.js local development: create `.env.local` and copy needed keys.
# 2. For CI: create repository secrets and write a minimal .env during workflow.
# 3. Never commit real JWT_SECRET / production DB credentials.
# 4. Add new variables with clear comments describing purpose and phase ownership.
